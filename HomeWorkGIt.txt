Была создана новая папка GIT
В папке GIT была создан новый файл HomeWorkGit
В папке GIT открыт bash
git init
git status
git add .
git commit -a "HW"
git branch new
git checkout new
git add .
git commit -m "new"
git checkout master
git branch second

Переименование master в main делаеться так git branch --move master main